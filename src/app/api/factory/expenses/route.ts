import type { NextRequest } from 'next/server';
import { forbiddenResponse, getVerifiedSession, unauthorizedResponse } from '@/lib/session';
import { isAdminRole } from '@/lib/authorization';
import { addCategory, createExpense, FactoryExpenseError, getExpenses, listCategories } from '@/services/factoryExpenseService';

export async function GET(request: NextRequest) { const session = await getVerifiedSession(request); if (!session) return unauthorizedResponse(); if (!isAdminRole(session.role)) return forbiddenResponse(); try { const data = request.nextUrl.searchParams.get('view') === 'categories' ? await listCategories(request.nextUrl.searchParams.get('includeInactive') !== '0') : await getExpenses(request.nextUrl.searchParams); return Response.json({ status: 'success', data }); } catch (error) { return Response.json({ status: 'error', message: error instanceof Error ? error.message : String(error) }, { status: 500 }); } }
export async function POST(request: NextRequest) { const session = await getVerifiedSession(request); if (!session) return unauthorizedResponse(); if (!isAdminRole(session.role) || !session.userId) return forbiddenResponse(); try { const body = await request.json(); const data = request.nextUrl.searchParams.get('view') === 'categories' ? await addCategory(body, session.userId) : await createExpense(body, session.userId); return Response.json({ status: 'success', data }, { status: 201 }); } catch (error) { const status = error instanceof FactoryExpenseError ? error.status : 500; return Response.json({ status: 'error', message: error instanceof Error ? error.message : String(error) }, { status }); } }
